<h1>Results Page</h1>

<% if (results.Response === "True") { %>
    <% results['Search'].forEach(function(movie){ %>
        <li>
            <strong><%=movie.Title%></strong> - <%=(movie.Year).split("–")[0]%>
        </li>
        <% })%>
<% } else {%>
    Nothing Found
    <% } %>



